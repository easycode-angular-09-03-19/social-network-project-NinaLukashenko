import { DirectivesModuleDirective } from './directives-module.directive';

describe('DirectivesModuleDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivesModuleDirective();
    expect(directive).toBeTruthy();
  });
});
