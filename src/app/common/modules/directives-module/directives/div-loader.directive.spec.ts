import { DivLoaderDirective } from './div-loader.directive';

describe('DivLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DivLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
